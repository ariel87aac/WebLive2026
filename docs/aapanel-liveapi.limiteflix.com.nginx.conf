server {
    listen 80;
    server_name liveapi.limiteflix.com;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name liveapi.limiteflix.com;

    # aaPanel normally manages these certificate paths from the SSL tab.
    ssl_certificate /www/server/panel/vhost/cert/liveapi.limiteflix.com/fullchain.pem;
    ssl_certificate_key /www/server/panel/vhost/cert/liveapi.limiteflix.com/privkey.pem;

    client_max_body_size 100m;

    location /socket.io/ {
        proxy_pass http://127.0.0.1:3010/socket.io/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
    }

    location / {
        proxy_pass http://127.0.0.1:3010;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 300s;
        proxy_send_timeout 300s;
    }
}
